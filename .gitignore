# ── Personal data never enters this repo (it is PUBLIC — profile README repo) ──
# The live store lives at ~/.life/ by default. These rules are belt-and-braces
# for anyone who points LIFE_HOME at the working tree.
.life/
data/
*.local.json
*.private.*
store.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/
